{
  "name": "${{ values.name }}",
  "private": true,
  "version": "0.1.0",
  "description": "Golden-path microservice (${{ values.stack }}) owned by ${{ values.owner }}",
  "scripts": {
    "start": "node --import tsx src/index.ts",
    "test": "vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
